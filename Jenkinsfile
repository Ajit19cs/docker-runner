pipeline {
    agent any

   
    stages {
        stage('run test ') {
            steps {
                bat "docker-compose up"
            }
        }

        
        stage('stop the docker image') {
            steps {
                bat "docker-compose up"
            }
        }

        
    }
}
